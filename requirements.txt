# Pinned to the versions the V9-V12 models were trained with (Kaggle, Python 3.11, SB3 2.9.0)
# Core (physics + verifier + env)
numpy>=1.26
scipy>=1.11
gymnasium>=0.29

# Dataset storage (Phase 3)
pyarrow>=14

# RL training / model loading (optional, heavy)
# stable-baselines3==2.9.0
# torch>=2.0

# Dev
pytest>=8.0
